# Phase 4.5 — cross-encoder reranking (D6).
#
# CPU-ONLY torch. The default PyPI torch wheel bundles CUDA libraries (~2.5 GB)
# that are dead weight on this machine - no usable GPU. The +cpu index cuts the
# download to ~200 MB.
#
# RAM cost, stated up front:
#   ms-marco-MiniLM-L-6-v2   22M params  ~90 MB    runs fine here
#   bge-reranker-v2-m3      568M params  ~2.2 GB   Colab, not this box
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu
sentence-transformers==3.3.1
